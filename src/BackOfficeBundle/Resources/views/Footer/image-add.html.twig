{# {% extends "::admin.html.twig" %}

{% block page_heading %}
<!-- Page Heading -->
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">
                Contenu du pied de page
            </h1>
            <ol class="breadcrumb">
                <li>
                    <i class="fa fa-dashboard"></i> <a href="{{ path('bo_homepage') }}">Accueil</a>
                </li>
                <li>
                    <i class="fa fa-hand-o-down"></i> <a href="{{ path('admin_footer_list') }}">Mon pied de page</a>
                </li>
                <li>
                    <i class="fa fa-align-left"></i> <a href="{{ path('admin_footer_read', {id: footerId}) }}">Contenu de mon pied de page</a>
                </li>
                <li>
                    <i class="fa fa-picture-o"></i> <a href="{{ path('admin_footer_image', {id: footerId}) }}">Images de mon contenu</a>
                </li>
                <li class="active">
                    <i class="fa fa-plus-square-o"></i> Ajouter une image
                </li>
            </ol>
        </div>
    </div>
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-lg-6 col-lg-offset-3">
			<div class="panel panel-default">
	            <div class="panel-heading">
	                <h3 class="panel-title"><i class="fa fa-users"></i> Formulaire</h3>
	            </div>
				{{form_start(form)}}

					{{form_errors(form)}}

					<div class="form-group">
			      		<label>
			      			{{ form_label(form.image) }}
			      		</label>
			        		{{ form_widget(form.image) }}
			      		<p class="help-block">
							{{ form_errors(form.image) }}
			      		</p>
				    </div>
				    <div class="form-group">
			      		<label>
			      			{{ form_label(form.link) }}
			      		</label>
			        		{{ form_widget(form.link) }}
			      		<p class="help-block">
							{{ form_errors(form.link) }}
			      		</p>
				    </div>
					{{form_widget(form.save)}}
				{{form_end(form)}}
			</div>
		</div>
    </div>
{% endblock %}
 #}